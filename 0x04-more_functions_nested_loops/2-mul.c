#include <stdio.h>
#include "main.h"

/**
 * mul - checks if a character is lowercase
 *
 * @a: the first integer
 *
 * @b: the second integer
 *
 * Return: 1 if c is lowercase, 0 otherwise
 */
int mul(int a, int b)
{

	return (a * b);
}
