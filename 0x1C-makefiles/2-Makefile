# Simple Makefile for building 'school' executable

CC = gcc
SRC = $(wildcard *.c)
OBJ = $(patsubst %.c,%.o,$(SRC))
NAME = school

all: $(NAME)

$(NAME): $(OBJ)
	$(CC) $(OBJ) -o $(NAME)

%.o: %.c
	$(CC)  -c -o $@ $<

clean:
	rm -f $(OBJ) $(NAME)

